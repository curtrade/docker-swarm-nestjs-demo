# Несекретная конфигурация приложения, поставляемая как docker config.
# Монтируется в контейнер как /app/.env и автоматически подхватывается configify.
# Здесь — только НЕсекретные значения (пароли и токены идут через secrets!).
APP_VERSION=2.0.0-secrets
